#!/usr/bin/env python
from sys import exit

#exit(1)

#exit("error message")

stdout,stderr = "stdout","stderr"#process.communicate()
#if process.returncode!=0:
exit(stderr)

try:
    exit(0)
except SystemExit,e:  # sys.exit(...)
    pass

