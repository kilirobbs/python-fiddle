#!/usr/bin/env python
from datetime import datetime, timedelta
from dateutil.relativedelta import relativedelta


now = datetime.now()
next=now + timedelta(days=70,hours=2)

print relativedelta(next, now).days
print relativedelta(now,next).days
